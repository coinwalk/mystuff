on linux debian based

sudo apt install python3 python3-cryptography python3-pyqt5 python3-pyqt5.qtwebengine python3-pyqt6 python3-pyqt6.qtwebengine python3-bs4

on windows download python 3.10.8

then make sure you set path on install and use for all users

include path button is a box at bottom of installer click that first then do the install for all users

and also do path limit at the end

then open a terminal be it either cmd or powrshell by right clicking on the start button and going to admin terminal/cdm/powershell

also need then type this in

pip install pyqt5 pyqtwebengine cryptography pyqt6 pyqt6-webengine bs4


then run the browser.py or .py files

gl

the snowybrowser.py is locked down with any password you chose make strong password and remember it

then login to just-dice.com before clicking "run snowybot"

gl also atm snowyrobomanual2.py is not completed

it needs security


